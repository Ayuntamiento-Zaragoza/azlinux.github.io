import{_ as a}from"./plugin-vue_export-helper-c27b6911.js";import{o as r,c as n,e as s,a as e,b as o,f as c}from"./app-ed7432bb.js";const t={},i=e("p",null,[o("A diferencia de SLED, en openSUSE, el servicio "),e("strong",null,"NetworkManager"),o(", se ejecuta en segundo plano durante el arranque del sistema. Esto significa que el "),e("em",null,"login"),o(" gráfico (servicio "),e("code",null,"xdm"),o(") está disponible para los usuarios sin tener la seguridad, en esos momentos, de que el equipo tiene la red configurada.")],-1),d=c(`<p>Como, precisamente, eso es lo que queremos asegurar en ese punto, hay que hacer un par de cambios en la configuración del sistema.</p><p>Para empezar, hay que editar el fichero <code>/etc/init.d/network</code>. En la función <code>start_nm_online</code>, hay que cambiar esta línea:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$NM_ONLINE_BIN --exit --timeout $NM_ONLINE_TIMEOUT
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>por esta otra:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$NM_ONLINE_BIN --timeout $NM_ONLINE_TIMEOUT
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Con esto conseguiremos que el comando <code>nm_online</code> no se ejecute en segundo plano. Es decir, que hasta que no concluya su ejecución, no se continúe con el resto del arranque.</p><p>Pero todavía queda otro problema que resolver. El servicio <code>xdm</code> arranca, en el proceso de arranque, antes de llegar a ejecutar <strong>NetworkManager</strong>. Esto se puede verificar comprobando el fichero <code>/var/log/boot.msg</code>.</p><p>Este comportamiento ocurre porque el sistema intenta (si no hay dependencias entre servicios que lo impidan) arrancar varios servicios en paralelo. Para modificar esta circunstancia, es necesario editar el fichero <code>/etc/sysconfig/boot</code> y cambiar el valor de la variable <code>RUN_PARALLEL</code> a &quot;<strong>no</strong>&quot;. Por último, para hacer efectivos los cambios en este archivo, es necesario ejecutar el comando <code>SuSEconfig</code>.</p>`,8);function l(u,m){return r(),n("div",null,[i,s(" more "),d])}const _=a(t,[["render",l],["__file","2009-10-29-como-configurar-networkmanager.html.vue"]]);export{_ as default};
